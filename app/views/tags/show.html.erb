<%= @tag %><br><br>

Kommende Events: <br>
<% @events.each do |event| %>
  <%= link_to event.name, event_path(event) %><br>
<% end %><br>

Informationen:<br>
<% @infos.each do |info| %>
    <%= link_to info.name, info_path(info) %><br>
<% end %>