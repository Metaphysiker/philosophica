Alle Events:

<table style="margin-left:auto;margin-right:auto;">
  <thead>
  <tr>
    <th>Name</th>
    <th>Inhalt</th>
    <th>Datum</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @events.each do |event| %>
      <tr>
        <td><%= event.name %></td>
        <td><%= event.content %></td>
        <td><%= event.date %></td>
      </tr>
  <% end %>
  </tbody>
</table>