<div class="userview">
  <h1><%= @user.username %></h1><br>

  <% if !@user.aboutme.nil? %>
      Über mich: <br>
    <%= @user.aboutme %>
  <% end %>
  <br>
  <% if (!current_user.id.nil?) && (@user.id == current_user.id) %>
      <%= link_to 'Profil bearbeiten', edit_user_registration_path %>
  <% end %>
  <br>
</div>