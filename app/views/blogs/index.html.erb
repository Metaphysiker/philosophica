<% @blogs.each do |blog| %>
  <%= link_to blog.name, philblog_path(blog.url) %> Erstellt von: <%= link_to getname(blog.user_id),profile_path(getname(blog.user_id) ) %><br>
  <%= blog.description %><br> <br>
<% end %><br><br>

<%= link_to "Erstellen Sie Ihren eigenen Blog!", new_blog_path %>