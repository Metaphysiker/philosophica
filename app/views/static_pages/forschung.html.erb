Forschung<br>
<div class="userview">
  <%= link_to "Früher ", forschung_path(date: @date - 1.month) %>
  <%= "#{@date.strftime("%B")} #{@date.year}" %>
  <%= link_to " Später ", forschung_path(date: @date + 1.month) %>
</div>

<table class="calendar">
<% @weeks.each do |week| %>
      <tr>
  <% week.each do |date| %>
              <% if date.month != @date.month %>
                  <td style="background-color: green">
                    <%= date.day %>
                  </td>
                  <% else %>
                <td>
    <%= date.day %>
                </td>
  <% end %>
        <% end %>
      </tr>
<% end %>
</table>